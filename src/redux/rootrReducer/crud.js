const initialState = [{ id: 1, name: "name", fName: "fName" }];

const type = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default type;
